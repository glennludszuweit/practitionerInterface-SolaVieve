import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  inputFields: {
    width: 250,
    margin: -8,
  },
  resize: {
    fontSize: 14,
  },
}));
